<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk
        android:minSdkVersion="31"
        tools:overrideLibrary="com.example.library1, com.example.library2" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SHIPWAR">

        <activity
            android:name=".win_player2"
            android:exported="false" />

        <activity
            android:name=".win_player1"
            android:exported="false" />

        <activity
            android:name=".ship_position_menu"
            android:exported="false" />

        <activity
            android:name=".ship_position_menu_2"
            android:exported="false" />

        <activity
            android:name=".activity_main"
            android:exported="false" />

        <activity
            android:name=".game_menu"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

    </application>

</manifest>